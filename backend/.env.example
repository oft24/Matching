PORT=4000
PYTHON_SERVICE_URL=http://localhost:8001

# PostgreSQL — recomendado: Neon (https://neon.tech) plan gratis
# Ejemplo: postgresql://usuario:password@ep-xxxx.us-east-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL="postgresql://USER:PASSWORD@POOLER:6543/postgres?pgbouncer=true&sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@POOLER:5432/postgres?sslmode=require"

JWT_SECRET="cambia-este-secreto-en-produccion"
JWT_EXPIRES_IN="7d"

# Correo de verificación — Resend (https://resend.com), 3000 correos/mes gratis.
# Sin RESEND_API_KEY el código se imprime en la consola del backend.
RESEND_API_KEY=""

# Debe usar un dominio verificado en resend.com/domains. El valor por defecto
# (onboarding@resend.dev) funciona sin dominio pero solo entrega al correo
# dueño de la cuenta de Resend.
MAIL_FROM="Matching <no-reply@tudominio.com>"

# Opcional: invitaciones Discord automáticas al confirmar match
# DISCORD_BOT_TOKEN=""
# DISCORD_GUILD_ID=""
